/**
 * next.config.mjs — Configuration Next.js pour apps/supplier
 * Dashboard fournisseur VIVRE.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
};

export default nextConfig;
