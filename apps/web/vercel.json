{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @vivre/database run generate && pnpm --filter @vivre/database run build && pnpm --filter @vivre/web run build",
  "outputDirectory": ".next",
  "crons": [
    { "path": "/api/cron/event-reminders", "schedule": "0 8 * * *" }
  ]
}
