{
  "name": "@vivre/database",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma ORM et schéma de base de données pour VIVRE",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.js",
      "default": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc src/index.ts --outDir dist --module NodeNext --moduleResolution NodeNext --target ES2022 --declaration --skipLibCheck",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "seed": "tsx src/seed.ts",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist node_modules/.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@vivre/config": "workspace:*",
    "@vivre/utils": "workspace:*",
    "prisma": "^5.14.0",
    "rimraf": "^5.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "prisma": {
    "seed": "tsx src/seed.ts"
  }
}
